export const button1 = (number) => {
    switch (number) {
        case 1: return false;
        default: return true;
    }
}
export const button1m = (number) => {
    switch (number) {
        case 0: case 1: return true;
        default: return false;
    }
}
export const button2 = (number) => {
    switch (number) {
        case 0: case 1: return true;
        default: return false;
    }
}
export const button3 = (number) => {
    switch (number) {
        case 0: case 1: return true;
        default: return false;
    }
}
export const button6 = (number) => {
    switch (number) {
        case 0: case 1: return true;
        default: return false;
    }
}